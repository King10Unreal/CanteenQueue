{
  "name": "canteen-queue-management",
  "version": "1.0.0",
  "private": true,
  "description": "Role-based campus canteen ordering and queue management demo.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
